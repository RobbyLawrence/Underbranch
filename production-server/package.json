{
  "name": "underbranch-production-server",
  "version": "1.0.0",
  "description": "Minimal production server to serve built frontend and provide Socket.IO collaboration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build-frontend": "cd ../frontend/editor && npm install && npm run build",
    "setup-auth": "cd ../signin && npm install",
    "full-setup": "npm run build-frontend && npm run setup-auth && npm install",
    "start-with-setup": "npm run full-setup && npm start"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "npm": "^11.6.0",
    "socket.io": "^4.7.2",
    "start": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
